---
version: '3'
services:
  freqtrade:
    container_name: freqtrade
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    image: trade_mexc:latest
    restart: "no"
    volumes:
        - "./trade.log:/trade.log"
    environment:
      - TOKEN=${TELEGRAM_TOKEN}
      - KEY=${BYBIT_KEY}
      - SECRET=${BYBIT_SECRET}
