---
version: '3'
services:
  freqtrade2:
      image: freqtradeorg/freqtrade:stable
      restart: unless-stopped
      container_name: MultiframeRSIStrategy
      dns:
        - 8.8.8.8
        - 1.1.1.1
      volumes:
        - "./user_data:/freqtrade/user_data"
      # ports:
      #   - "8080:8080"
      command: >
        trade
        --logfile /freqtrade/user_data/logs/multiframe_strategy.log
        --db-url sqlite:////freqtrade/user_data/multiframe_strategy.sqlite
        --config /freqtrade/user_data/config.json
        --strategy MultiframeRSIStrategy
