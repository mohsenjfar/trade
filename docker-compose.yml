---
version: '3'
services:
  # freqtrade:
  #     image: freqtradeorg/freqtrade:develop_freqai
  #     restart: unless-stopped
  #     container_name: FreqaiHybridStrategy
  #     # ports:
  #     #   - "127.0.0.1:8080:8080"
  #     volumes:
  #       - "./user_data:/freqtrade/user_data"
  #     command: >
  #       trade
  #       --logfile /freqtrade/user_data/logs/FreqaiHybridStrategy.log
  #       --db-url sqlite:////freqtrade/user_data/db.sqlite
  #       --config /freqtrade/user_data/config_xgboost.json
  #       --strategy FreqaiHybridStrategy
  #       --freqaimodel XGBoostClassifier
  freqtrade:
      image: freqtradeorg/freqtrade:stable
      restart: unless-stopped
      container_name: IchimokuFuturesStrategy
      # ports:
      #   - "127.0.0.1:8080:8080"
      volumes:
        - "./user_data:/freqtrade/user_data"
      command: >
        trade
        --logfile /freqtrade/user_data/logs/ichimoku_futures_strategy.log
        --db-url sqlite:////freqtrade/user_data/ichimoku_futures_strategy_db.sqlite
        --config /freqtrade/user_data/ichimoku_futures_strategy_config.json
        --strategy IchimokuFuturesStrategy