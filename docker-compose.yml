---
version: '3'
services:
  mexc:
    container_name: mexc
    build:
      context: .
      dockerfile: Dockerfile
    # image: trade_mexc:latest
    restart: "no"
    volumes:
        - "./mexc.log:/mexc.log"
    environment:
      - TOKEN=${TOKEN}
      - KEY=${KEY}
      - SECRET=${SECRET}